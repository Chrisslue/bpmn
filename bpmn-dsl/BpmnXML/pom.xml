<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <artifactId>parent</artifactId>
    <groupId>de.monticore.lang</groupId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>workflow-xml</artifactId>

  <properties>
    <maven.jaxb2.version>0.14.0</maven.jaxb2.version>
    <maven.jaxb2.prefix.version>1.3</maven.jaxb2.prefix.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>javax.xml.bind</groupId>
      <artifactId>jaxb-api</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.jvnet.jaxb2.maven2</groupId>
        <artifactId>maven-jaxb2-plugin</artifactId>
        <version>${maven.jaxb2.version}</version>
        <executions>
          <execution>
            <configuration>
              <!-- See docs: https://github.com/highsource/maven-jaxb2-plugin/wiki/Configuration-Cheat-Sheet -->
              <!-- <schemaDirectory>src/main/resources</schemaDirectory> -->
              <!--
              <schemas>
                <schema>
                  <url>https://www.omg.org/spec/BPMN/20100501/BPMN20.xsd</url>
                </schema>
              </schemas>
              -->
              <!-- <bindingDirectory>src/main/resources</bindingDirectory> -->
              <bindingIncludes>
                <include>bindings.xml</include>
              </bindingIncludes>
              <!-- <generateDirectory>${project.build.directory}/generated-sources/xjc</generateDirectory> -->
              <!-- <generatePackage>workflow</generatePackage> -->
              <args>
                <arg>-extension</arg>
                <arg>-Xnamespace-prefix</arg>
              </args>
            </configuration>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>org.jvnet.jaxb2_commons</groupId>
            <artifactId>jaxb2-namespace-prefix</artifactId>
            <version>${maven.jaxb2.prefix.version}</version>
          </dependency>
        </dependencies>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>schemas</id>
            <goals>
              <goal>single</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <descriptors>
                <descriptor>src/main/assembly/schema.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
          <!-- skip grammars assembly inherited from parent POM -->
          <execution>
            <id>grammars</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>