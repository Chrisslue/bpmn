<project xmlns="http://maven.apache.org/POM/4.0.0" 
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- == PROJECT COORDINATES ================================================ -->

  <modules>
    <module>WorkflowDSL</module>
    <module>WorkflowCLI</module>
    <module>TimeExpressions</module>
    <module>BpmnXML</module>
  </modules>

  <groupId>de.monticore.lang</groupId>
  <artifactId>parent</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>


  <!-- == PROPERTIES ========================================================= -->

  <properties>
    <!-- .. Libraries ........................................................ -->

    <monticore.version>5.0.6</monticore.version>
    <antlr.version>4.7.2</antlr.version>
    <mc.grammars.assembly.version>0.0.6-SNAPSHOT</mc.grammars.assembly.version>

    <ocl.verion>1.10.0-SNAPSHOT</ocl.verion>
    <workflow.version>${project.version}</workflow.version>
    <time.expressions.version>${project.version}</time.expressions.version>
    <petrinets.version>0.0.1-SNAPSHOT</petrinets.version>
    <apt.version>0.0.1</apt.version>
    <exec.version>1.11</exec.version>

    <emf.ecore.version>2.19.0</emf.ecore.version>
    <graphviz.java.version>0.11.0</graphviz.java.version>

    <guava.version>25.1-jre</guava.version>
    <gson.version>2.8.5</gson.version>
    <joda.time.version>2.10.4</joda.time.version>
    <jgrapht.version>1.3.1</jgrapht.version>
    <se-commons.version>1.7.9</se-commons.version>
    <picoli.version>4.1.0</picoli.version>

    <jaxb.api.version>2.3.1</jaxb.api.version>
    <jaxb.core.version>2.3.0.1</jaxb.core.version>
    <jaxb.impl.version>2.3.2</jaxb.impl.version>

    <logback.version>1.2.3</logback.version>

    <junit.jupiter.version>5.5.2</junit.jupiter.version>

    <jsr305.version>3.0.2</jsr305.version>

    <!-- .. Plugins .......................................................... -->

    <maven.monticore.version>${monticore.version}</maven.monticore.version>

    <maven.compiler.version>3.8.1</maven.compiler.version>
    <maven.source.version>3.1.0</maven.source.version>
    <maven.surefire.version>2.22.2</maven.surefire.version>
    <maven.install.version>3.0.0-M1</maven.install.version>
    <maven.dependency.version>3.1.1</maven.dependency.version>
    <maven.assembly.version>3.1.1</maven.assembly.version>
    <maven.release.version>2.5.3</maven.release.version>
    <maven.shade.version>3.2.1</maven.shade.version>
    <maven.javadoc.version>3.1.1</maven.javadoc.version>
    <maven.build.helper.version>3.0.0</maven.build.helper.version>

    <!-- Classifiers -->

    <grammars.classifier>grammars</grammars.classifier>

    <!-- .. Misc ............................................................. -->

    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>1.8</java.version>
    <wagon.provider.version>3.3.3</wagon.provider.version>

  </properties>


  <!-- == DEPENDENCY MANAGEMENT ============================================== -->

  <dependencyManagement>
    <dependencies>
      <!-- .. Common ......................................................... -->
      <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>${guava.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.code.gson</groupId>
        <artifactId>gson</artifactId>
        <version>${gson.version}</version>
      </dependency>
      <dependency>
        <groupId>joda-time</groupId>
        <artifactId>joda-time</artifactId>
        <version>${joda.time.version}</version>
      </dependency>
      <dependency>
        <groupId>info.picocli</groupId>
        <artifactId>picocli</artifactId>
        <version>${picoli.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jgrapht</groupId>
        <artifactId>jgrapht-core</artifactId>
        <version>${jgrapht.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jgrapht</groupId>
        <artifactId>jgrapht-io</artifactId>
        <version>${jgrapht.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jgrapht</groupId>
        <artifactId>jgrapht-guava</artifactId>
        <version>${jgrapht.version}</version>
      </dependency>

      <dependency>
        <groupId>javax.xml.bind</groupId>
        <artifactId>jaxb-api</artifactId>
        <version>${jaxb.api.version}</version>
      </dependency>
      <dependency>
        <groupId>com.sun.xml.bind</groupId>
        <artifactId>jaxb-core</artifactId>
        <version>${jaxb.core.version}</version>
      </dependency>
      <dependency>
        <groupId>com.sun.xml.bind</groupId>
        <artifactId>jaxb-impl</artifactId>
        <version>${jaxb.impl.version}</version>
      </dependency>

      <!-- .. SE Libraries ................................................... -->
      <dependency>
        <groupId>de.se_rwth.commons</groupId>
        <artifactId>se-commons-utilities</artifactId>
        <version>${se-commons.version}</version>
      </dependency>
      <dependency>
        <groupId>de.se_rwth.commons</groupId>
        <artifactId>se-commons-groovy</artifactId>
        <version>${se-commons.version}</version>
      </dependency>
      <dependency>
        <groupId>de.se_rwth.commons</groupId>
        <artifactId>se-commons-logging</artifactId>
        <version>${se-commons.version}</version>
      </dependency>

      <!-- .. MontiCore ...................................................... -->
      <dependency>
        <groupId>org.antlr</groupId>
        <artifactId>antlr4-runtime</artifactId>
        <version>${antlr.version}</version>
      </dependency>
      <dependency>
        <groupId>de.monticore.lang</groupId>
        <artifactId>workflow</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>de.monticore.lang</groupId>
        <artifactId>workflow-xml</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>de.monticore.lang</groupId>
        <artifactId>workflow-xml</artifactId>
        <classifier>schemas</classifier>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>de.monticore.expressions</groupId>
        <artifactId>time</artifactId>
        <version>${time.expressions.version}</version>
      </dependency>
      <dependency>
        <groupId>de.monticore.expressions</groupId>
        <artifactId>time</artifactId>
        <version>${time.expressions.version}</version>
        <classifier>${grammars.classifier}</classifier>
        <scope>provided</scope>
      </dependency>

      <!-- .. Petri nets ..................................................... -->
      <dependency>
        <groupId>de.monticore.lang</groupId>
        <artifactId>petrinets</artifactId>
        <version>${petrinets.version}</version>
      </dependency>
      <dependency>
        <groupId>org.zeroturnaround</groupId>
        <artifactId>zt-exec</artifactId>
        <version>${exec.version}</version>
      </dependency>
<!--      <dependency>
        <groupId>guru.nidi</groupId>
        <artifactId>graphviz-java</artifactId>
        <version>${graphviz.java.version}</version>
      </dependency>-->


      <!-- MontiCore version managed modules -->
      <!--
      <dependency>
        <groupId>de.monticore</groupId>
        <artifactId>monticore-runtime</artifactId>
        <version>${monticore.version}</version>
      </dependency>
      <dependency>
        <groupId>org.antlr</groupId>
        <artifactId>antlr4-runtime</artifactId>
        <version>${antlr.version}</version>
      </dependency>
      <dependency>
        <groupId>de.monticore</groupId>
        <artifactId>monticore-grammar</artifactId>
        <version>${monticore.version}</version>
      </dependency>
      <dependency>
        <groupId>de.monticore</groupId>
        <artifactId>monticore-grammar</artifactId>
        <version>${monticore.version}</version>
        <classifier>${grammars.classifier}</classifier>
        <scope>provided</scope>
      </dependency>
      -->
      <!-- .. Testing ........................................................ -->
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-api</artifactId>
        <version>${junit.jupiter.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-engine</artifactId>
        <version>${junit.jupiter.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>de.monticore</groupId>
        <artifactId>monticore-runtime</artifactId>
        <version>${monticore.version}</version>
        <type>test-jar</type>
        <scope>test</scope>
      </dependency>

      <!-- .. Logging ........................................................ -->
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
      </dependency>

      <!-- .. Reporting ...................................................... -->
      <dependency>
        <groupId>com.google.code.findbugs</groupId>
        <artifactId>jsr305</artifactId>
        <version>${jsr305.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>


  <!-- == PLUGIN MANAGEMENT ================================================== -->
  <!-- https://stackoverflow.com/questions/15886209/maven-is-not-working-in-java-8-when-javadoc-tags-are-incomplete?answertab=votes#tab-top -->
  <profiles>
    <profile>
      <id>java8-doclint-disabled</id>
      <activation>
        <jdk>[1.8,)</jdk>
      </activation>
      <properties>
        <javadoc.opts>-Xdoclint:none</javadoc.opts>
      </properties>
    </profile>
  </profiles>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>de.monticore.mojo</groupId>
          <artifactId>monticore-maven-plugin</artifactId>
          <version>${maven.monticore.version}</version>
          <executions>
            <execution>
              <configuration>
                <script>de/monticore/monticore_noemf.groovy</script>
              </configuration>
              <goals>
                <goal>generate</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.version}</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.version}</version>
          <configuration>
            <classpathDependencyExcludes>
              <classpathDependencyExclude>de.monticore:monticore-cli</classpathDependencyExclude>
            </classpathDependencyExcludes>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>${maven.source.version}</version>
          <executions>
            <execution>
              <phase>package</phase>
              <goals>
                <goal>jar-no-fork</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-release-plugin</artifactId>
          <version>${maven.release.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>${maven.dependency.version}</version>
        </plugin>

        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>build-helper-maven-plugin</artifactId>
          <version>${maven.build.helper.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-assembly-plugin</artifactId>
          <version>${maven.assembly.version}</version>
          <dependencies>
            <dependency>
              <groupId>de.monticore</groupId>
              <artifactId>mc-assemblies</artifactId>
              <version>${mc.grammars.assembly.version}</version>
            </dependency>
          </dependencies>
          <executions>
            <execution>
              <id>grammars</id>
              <configuration>
                <finalName>${project.artifactId}-${project.version}</finalName>
                <descriptorRefs>
                  <descriptorRef>grammars-assembly</descriptorRef>
                </descriptorRefs>
                <formats>
                  <format>jar</format>
                </formats>
              </configuration>
              <phase>package</phase>
              <goals>
                <goal>single</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${maven.shade.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>${maven.javadoc.version}</version>
          <configuration>
            <doclint>none</doclint>
          </configuration>
        </plugin>

      </plugins>
    </pluginManagement>
  </build>


  <!-- == REPORTING ========================================================== -->

  <reporting>
    <plugins>
      <plugin>
        <groupId>de.monticore.mojo</groupId>
        <artifactId>monticore-maven-plugin</artifactId>
        <version>${maven.monticore.version}</version>
        <reportSets>
          <reportSet>
            <reports>
              <report>reporting-report</report>
            </reports>
          </reportSet>
        </reportSets>
      </plugin>
    </plugins>
  </reporting>


  <!-- == DISTRIBUTION ==================================================== -->

  <repositories>
    <repository>
      <id>se-nexus</id>
      <url>https://nexus.se.rwth-aachen.de/content/groups/public/</url>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>se-nexus</id>
      <url>https://nexus.se.rwth-aachen.de/content/groups/public/</url>
    </pluginRepository>
  </pluginRepositories>

</project>
